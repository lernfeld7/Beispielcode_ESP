# Serielle Kommunikation vom ESP8266 zum Arduino

Auf dem NodeMCU ist ein zweiter serieller Kanal verwendbar (Transmit Only, kein RX-Pin), mit dem man z. B. einem Arduino-Microcontroller Befehle geben kann.

Dieses Beispiel enthält eine Datei für den ESP8266 als Sender (TX) und für den Arduino als Empfänger (RX).